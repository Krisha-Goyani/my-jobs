import React from 'react';

const StyledHeading = ({ children }) => {
    return (
        <h2 className="text-xl font-medium font-circular-std">{children}</h2>
    );
};

export default StyledHeading; 